# blockme-ingest

Horizon payment streaming (SSE) + durable `ingest_cursor`. Attributes deposits by muxed id or
memo id, records them, and fires webhooks. Restart-safe and replayable. Implemented in Step 8.
